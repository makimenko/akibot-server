import { Vector3D } from "../common/index";

export class NodeTransformation3D {

    private position: Vector3D = new Vector3D(0, 0, 0);
    private scale: Vector3D = new Vector3D(1, 1, 1);
    private rotation: Vector3D = new Vector3D(0, 0, 0);

}
